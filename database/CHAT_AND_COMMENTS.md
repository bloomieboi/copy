# Функционал чата и комментариев сотрудника

## Комментарий сотрудника (отчет о работе)

### Где виден комментарий

**Для клиента:**
- ✅ Отображается на странице `profile/order_detail.php`
- ✅ Показывается в отдельном блоке с заголовком "Отчет сотрудника о выполненной работе"
- ✅ Оформлен как информационный alert (синий фон)
- ✅ Видим ТОЛЬКО если комментарий был добавлен

**Для сотрудника:**
- Добавляется через форму на `employee/order_detail.php`
- Обязателен при смене статуса на "Завершен" или "Отменен"

**Для администратора:**
- Виден в истории изменений заказа

### Когда добавляется

1. Сотрудник завершает работу над заказом
2. При смене статуса требуется заполнить поле "Комментарий сотрудника"
3. Комментарий сохраняется в `order_log` с типом `employee_comment`
4. Клиент видит этот комментарий на странице деталей заказа

### Пример

```
Заказ #123

Отчет сотрудника о выполненной работе
ℹ️ Выполнена печать 50 листов формата А4.
   Использована цветная печать.
   Документы готовы к выдаче.
```

## Функционал чата

### Правило доступа: чат начинает ТОЛЬКО сотрудник

**Клиент:**
- ❌ НЕ может начать чат первым
- ✅ Может отвечать после того, как сотрудник напишет первое сообщение
- ✅ Видит сообщение: "Чат еще не начат. Сотрудник, обрабатывающий ваш заказ, свяжется с вами в чате при необходимости."

**Сотрудник:**
- ✅ МОЖЕТ начать чат первым
- ✅ Видит подсказку: "Вы можете начать чат с клиентом"
- ✅ Кнопка меняется: "Начать чат" → "Отправить"

### Статус доступности чата

**Чат доступен только при статусе заказа = 6 ("В работе")**

| Статус заказа | Чат доступен? |
|--------------|---------------|
| В процессе оплаты (1) | ❌ |
| Оплачен (2) | ❌ |
| Завершен (3) | ❌ |
| Закрыт (4) | ❌ |
| Отменен (5) | ❌ |
| В работе (6) | ✅ |

### Процесс работы с чатом

#### Для клиента:

1. **Заказ оплачен** → Статус "В работе"
2. **Кнопка "Чат с сотрудником"** появляется на странице заказа
3. **До начала чата:**
   - Кнопка серая (btn-secondary)
   - Подпись: "(Ждет сообщения)"
   - При клике: Предупреждение "Чат еще не начат"
4. **После первого сообщения сотрудника:**
   - Кнопка синяя (btn-primary)
   - Если есть непрочитанные: красный badge с числом
   - Форма ввода сообщений активна

#### Для сотрудника:

1. **Берет заказ в работу** → Статус "В работе"
2. **Страница заказа** → Кнопка "Чат с клиентом"
3. **Может начать чат:**
   - Видит: "Вы можете начать чат с клиентом"
   - Placeholder: "Напишите первое сообщение клиенту..."
   - Кнопка: "Начать чат"
4. **После отправки:**
   - Чат становится активным
   - Кнопка: "Отправить"
   - Клиент теперь может отвечать

### Индикаторы состояния

**Кнопка "Чат с сотрудником" у клиента:**

```html
<!-- Чат не начат -->
<a href="..." class="btn btn-secondary">
    <i class="bi bi-chat-dots"></i>
    Чат с сотрудником
    <small>(Ждет сообщения)</small>
</a>

<!-- Чат начат -->
<a href="..." class="btn btn-primary">
    <i class="bi bi-chat-dots"></i>
    Чат с сотрудником
</a>

<!-- Есть непрочитанные -->
<a href="..." class="btn btn-primary">
    <i class="bi bi-chat-dots"></i>
    Чат с сотрудником
    <span class="badge bg-danger">3</span>
</a>
```

### Пометка прочитанных сообщений

Автоматически при открытии страницы чата:
```php
UPDATE message 
SET is_read = 1 
WHERE order_id = ? AND to_user_id = ?
```

### База данных

**Таблица `message`:**
```sql
CREATE TABLE message (
  message_id INT PRIMARY KEY AUTO_INCREMENT,
  order_id INT NOT NULL,
  from_user_id INT NOT NULL,
  to_user_id INT NOT NULL,
  message_text TEXT NOT NULL,
  is_read TINYINT(1) DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Проверка начала чата:**
```sql
SELECT COUNT(*) as count 
FROM message 
WHERE order_id = ?
```

Если `count > 0` → чат начат

### Логирование

При отправке сообщения добавляется запись в `order_log`:

**От клиента:**
```php
addOrderLog($orderId, $userId, 'message_sent', 'Клиент отправил сообщение');
```

**От сотрудника:**
```php
addOrderLog($orderId, $userId, 'message_sent', 'Сотрудник отправил сообщение клиенту');
```

## Файлы, затронутые изменениями

### Клиентская часть:
- ✅ `profile/order_detail.php` - отображение комментария + кнопка чата
- ✅ `profile/chat.php` - ограничение доступа к отправке

### Сотрудник:
- ✅ `employee/order_detail.php` - форма добавления комментария (уже была)
- ✅ `employee/chat.php` - подсказка о начале чата

### Стили:
- ✅ `css/style.css` - стили для empty-chat

## Проверка функционала

### Тест 1: Комментарий сотрудника
1. Сотрудник берет заказ в работу
2. Добавляет комментарий при завершении
3. Клиент открывает детали заказа
4. ✅ Видит блок "Отчет сотрудника о выполненной работе"

### Тест 2: Начало чата
1. Заказ в статусе "В работе"
2. Клиент открывает чат
3. ✅ Видит: "Чат еще не начат"
4. ✅ Форма ввода недоступна
5. Сотрудник отправляет первое сообщение
6. Клиент обновляет страницу
7. ✅ Форма ввода активна

### Тест 3: Непрочитанные сообщения
1. Сотрудник пишет сообщение
2. Клиент на странице заказа
3. ✅ Видит badge с количеством (красный)
4. Клиент открывает чат
5. ✅ Badge исчезает

## UI/UX улучшения

### Иконки:
- 💬 `bi-chat-dots` - иконка чата
- 📨 `bi-send` - кнопка отправки
- ℹ️ `bi-info-circle` - информационные сообщения
- 💬 `bi-chat-left-text` - комментарий сотрудника

### Цвета:
- Синий (primary) - активный чат
- Серый (secondary) - чат не начат
- Красный (danger) - непрочитанные сообщения
- Голубой (info) - информационные блоки

---

**Дата:** 2026-02-09  
**Версия:** 1.1  
**Статус:** ✅ Внедрено
