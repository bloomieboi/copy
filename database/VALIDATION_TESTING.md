# Краткое руководство по валидации

## 📋 Быстрый тест

### 1. Регистрация

**Валидные данные:**
```
Логин:   john_doe
Пароль:  MyPass123!
Телефон: 79123456789
```
✅ Должно работать

**Невалидные данные для проверки:**

| Поле | Значение | Ожидаемая ошибка |
|------|----------|------------------|
| Логин | `пользователь` | "Логин должен содержать только английские буквы" |
| Логин | `user@mail` | "Логин может содержать только английские буквы, цифры и _" |
| Логин | `very_long_username_exceeding_limit_of_thirty_characters` | "Логин не должен превышать 30 символов" |
| Пароль | `12345` | "Пароль должен содержать хотя бы одну английскую букву" |
| Пароль | `abc` | "Пароль должен содержать минимум 6 символов" |
| Пароль | `Пароль123` | "Пароль может содержать только латинские буквы" |
| Телефон | `89123456789` | "Номер телефона должен быть в формате 79xxxxxxxxx" |
| Телефон | `71234567890` | "Номер телефона должен быть в формате 79xxxxxxxxx" |
| Телефон | `7912345678` | "Номер должен содержать 11 цифр" |

---

### 2. Оформление заказа

**Тест лимитов количества:**

| Количество | Цена | Результат |
|------------|------|-----------|
| `0` | любая | ❌ "Количество должно быть больше нуля" |
| `1` | 10 руб | ✅ Стоимость: 10 руб |
| `1000` | 10 руб | ✅ Стоимость: 10,000 руб |
| `1000000` | 10 руб | ✅ Стоимость: 10,000,000 руб |
| `1000001` | 10 руб | ❌ "Количество не может превышать 1 000 000 ед." |
| `2147483648` | любая | ❌ "Количество не может превышать 1 000 000 ед." |

**Тест переполнения стоимости:**

| Количество | Цена | Стоимость | Результат |
|------------|------|-----------|-----------|
| `1000000` | 1000 | 1,000,000,000 | ✅ Допустимо |
| `1000000` | 2148 | 2,148,000,000 | ❌ "Стоимость заказа слишком велика" |
| `1000000` | 10000 | 10,000,000,000 | ❌ "Стоимость заказа слишком велика" |

**Тест адреса:**

| Адрес | Результат |
|-------|-----------|
| Не выбран | ❌ "Необходимо выбрать адрес организации" |
| Выбран любой | ✅ Заказ создан |

---

## 🔍 Автоматическая валидация

### Телефон (автоформатирование)

Введите → Результат:
```
9123456789  → 79123456789  (автодобавление 79)
89123456789 → 79123456789  (замена 8 на 79)
7 91234     → 791234       (оставляет как есть)
```

### Логин (только латиница)

Введите → Результат:
```
john        → john         ✅
john_doe    → john_doe     ✅
джон        → (блокируется) ❌
user@mail   → (блокируется) ❌
```

---

## 🎯 Проверка в браузере

### DevTools Console

```javascript
// Проверка валидации количества
const qty = document.getElementById('quantity');
qty.value = 2000000;
qty.reportValidity(); // Должна показаться ошибка

// Проверка расчета стоимости
qty.value = 100;
// Проверьте, обновилась ли итоговая сумма
```

### Network Tab

```
POST /orders/create.php
→ Проверьте:
   - quantity передается как integer
   - phone_number в формате 79xxxxxxxxx
   - login содержит только латиницу
```

---

## ⚠️ Важные проверки

✅ **Обязательно протестируйте:**

1. Регистрацию с кириллицей
2. Телефон, начинающийся с 8
3. Пароль без букв (только цифры)
4. Количество больше 1 миллиона
5. Заказ без выбора адреса
6. Попытку создать заказ на огромную сумму

❌ **НЕ должно работать:**

- Логин: `пользователь123`
- Телефон: `89123456789`
- Пароль: `12345`
- Количество: `2000000`
- Адрес: не выбран

---

## 📝 SQL для теста (опционально)

```sql
-- Проверка созданного пользователя
SELECT login, phone_number, role_id 
FROM user_ 
WHERE login = 'john_doe';

-- Проверка созданного заказа
SELECT order_id, service_list, price, quantity
FROM order_
WHERE user_id = (SELECT id_user FROM user_ WHERE login = 'john_doe');

-- Проверка максимальных значений
SELECT 
    MAX(price) as max_price,
    COUNT(*) as total_orders
FROM order_;
```

---

**Рекомендация:** Протестируйте каждый невалидный кейс вручную, чтобы убедиться в корректности сообщений об ошибках!
