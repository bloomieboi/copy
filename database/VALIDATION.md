# Валидация данных в системе КопиПейст

## Обзор

Комплексная валидация данных на клиентской (JavaScript) и серверной (PHP) стороне для обеспечения безопасности и целостности данных.

---

## 1. Валидация регистрации

### Файл: `register/index.php`

### Правила валидации

#### 📱 Номер телефона
**Формат:** `79xxxxxxxxx` (11 цифр)

**Серверная валидация:**
```php
if (!preg_match('/^79\d{9}$/', $phone)) {
    $error = "Номер телефона должен быть в формате 79xxxxxxxxx";
}
```

**Клиентская валидация:**
```javascript
// HTML
<input type="tel" 
       pattern="79[0-9]{9}"
       maxlength="11"
       placeholder="79123456789">

// JavaScript - автоформатирование
phoneInput.addEventListener('input', function() {
    let value = this.value.replace(/\D/g, ''); // Убираем нецифровые
    
    // Автозаполнение 79
    if (value[0] !== '7') value = '7' + value;
    if (value.substring(0, 2) !== '79') {
        value = '79' + value.substring(2);
    }
    
    this.value = value.substring(0, 11);
});
```

**Примеры:**
- ✅ `79123456789`
- ❌ `89123456789` (начинается с 8)
- ❌ `71234567890` (не начинается с 79)
- ❌ `7912345678` (меньше 11 цифр)

#### 👤 Логин
**Правила:**
- Только английские буквы (a-z, A-Z)
- Цифры (0-9)
- Подчеркивание (_)
- Максимум 30 символов
- Должен начинаться с буквы

**Серверная валидация:**
```php
if (!preg_match('/^[a-zA-Z][a-zA-Z0-9_-]*$/', $login)) {
    $error = "Логин должен содержать только английские буквы, цифры, _ и начинаться с буквы";
}
if (strlen($login) > 30) {
    $error = "Логин не должен превышать 30 символов";
}
```

**Клиентская валидация:**
```javascript
// HTML
<input type="text" 
       pattern="[a-zA-Z0-9_]+"
       maxlength="30"
       placeholder="example_user123">

// JavaScript
loginInput.addEventListener('input', function() {
    if (!/^[a-zA-Z0-9_]*$/.test(this.value)) {
        this.setCustomValidity('Только английские буквы, цифры и _');
    } else {
        this.setCustomValidity('');
    }
});
```

**Примеры:**
- ✅ `user123`
- ✅ `john_doe`
- ✅ `Administrator`
- ❌ `пользователь` (кириллица)
- ❌ `user@123` (спецсимвол @)
- ❌ `very_long_username_exceeding_thirty_characters`

#### 🔑 Пароль
**Правила:**
- Только английские буквы, цифры и спецсимволы
- Минимум 6 символов (было 3, увеличено для безопасности)
- Хотя бы одна английская буква

**Серверная валидация:**
```php
if (strlen($password) < 6) {
    $error = "Пароль должен содержать минимум 6 символов";
}
if (!preg_match('/^[a-zA-Z0-9!@#$%^&*()_+=\-{}\[\]:;"\'<>,.?\/\\|`~]+$/', $password)) {
    $error = "Пароль должен содержать только английские буквы, цифры и спецсимволы";
}
if (!preg_match('/[a-zA-Z]/', $password)) {
    $error = "Пароль должен содержать хотя бы одну английскую букву";
}
```

**Клиентская валидация:**
```javascript
passwordInput.addEventListener('input', function() {
    const value = this.value;
    if (value.length > 0 && value.length < 6) {
        this.setCustomValidity('Пароль должен содержать минимум 6 символов');
    } else if (!/[a-zA-Z]/.test(value)) {
        this.setCustomValidity('Пароль должен содержать хотя бы одну букву');
    } else {
        this.setCustomValidity('');
    }
});
```

**Примеры:**
- ✅ `Pass123!`
- ✅ `MySecurePassword2024`
- ✅ `admin@123`
- ❌ `12345` (нет букв)
- ❌ `abc` (меньше 6 символов)
- ❌ `Пароль123` (кириллица)

### Проверка уникальности

```php
// Логин
$stmt = $pdo->prepare("SELECT id_user FROM user_ WHERE login = ?");
$stmt->execute([$login]);
if ($stmt->fetch()) {
    $error = "Пользователь с таким логином уже существует";
}

// Телефон
$stmt = $pdo->prepare("SELECT id_user FROM user_ WHERE phone_number = ?");
$stmt->execute([$phone]);
if ($stmt->fetch()) {
    $error = "Пользователь с таким номером телефона уже существует";
}
```

---

## 2. Валидация оформления заказа

### Файл: `orders/create.php`

### Правила валидации

#### 📦 Количество услуг
**Ограничения базы данных:**
- MySQL INT: `-2,147,483,648` до `2,147,483,647`
- Практический лимит: `1,000,000` единиц

**Серверная валидация:**
```php
$maxQuantity = 1000000; // Разумный предел

if ($quantity <= 0) {
    $error = 'Количество должно быть больше нуля.';
} elseif ($quantity > $maxQuantity) {
    $error = 'Количество не может превышать 1 000 000 единиц. 
              Для больших заказов свяжитесь с администратором.';
} elseif (!is_numeric($quantity)) {
    $error = 'Количество должно быть числом.';
}
```

**Клиентская валидация:**
```javascript
const MAX_QUANTITY = 1000000;
const MAX_PRICE = 2147483647; // INT макс MySQL

function validateQuantity() {
    const quantity = parseInt(quantityInput.value) || 0;
    const basePrice = parseFloat(basePriceInput.value) || 0;
    
    // Проверка количества
    if (quantity <= 0) {
        quantityInput.setCustomValidity('Количество должно быть больше нуля');
        return false;
    }
    if (quantity > MAX_QUANTITY) {
        quantityInput.setCustomValidity('Количество не может превышать 1 000 000 ед.');
        return false;
    }
    
    // Проверка стоимости
    const totalPrice = basePrice * quantity;
    if (totalPrice > MAX_PRICE) {
        quantityInput.setCustomValidity(
            'Стоимость заказа превышает максимум (2 147 483 647 руб.). Уменьшите количество.'
        );
        return false;
    }
    
    quantityInput.setCustomValidity('');
    return true;
}
```

**HTML:**
```html
<input type="number" 
       name="quantity" 
       id="quantity" 
       min="1" 
       max="2147483647"
       step="1" 
       value="1" 
       required>
```

#### 💰 Стоимость заказа
**Проверка переполнения:**

```php
$calculatedPrice = $basePrice * $quantity;

// Проверка переполнения FLOAT
if ($calculatedPrice > PHP_FLOAT_MAX || !is_finite($calculatedPrice)) {
    $error = 'Стоимость заказа превышает максимально допустимое значение.';
}

// Проверка лимита INT (для БД)
if ($calculatedPrice > $maxInt) {
    $error = 'Стоимость заказа слишком велика. 
              Максимальная сумма: ' . number_format($maxInt, 2, '.', ' ') . ' руб.';
}
```

**Примеры расчетов:**
```
Цена = 10 руб., Количество = 100
→ Стоимость = 1,000 руб. ✅

Цена = 1,000 руб., Количество = 1,000,000
→ Стоимость = 1,000,000,000 руб. ✅

Цена = 10,000 руб., Количество = 1,000,000
→ Стоимость = 10,000,000,000 руб. ❌ (превышает INT макс)
```

#### 📍 Адрес организации
**Обязательное поле:**

```php
if ($addressId <= 0) {
    $error = 'Необходимо выбрать адрес организации';
}
```

```html
<select name="address_id" id="address_id" required>
    <option value="">Выберите адрес</option>
    <?php foreach($addresses as $addr): ?>
        <option value="<?= $addr['address_id'] ?>">
            <?= htmlspecialchars($addr['address_name']) ?>
        </option>
    <?php endforeach; ?>
</select>
```

---

## 3. UI/UX улучшения

### Tooltips (подсказки)

```html
<label for="login">
    Логин (английские буквы, цифры, _):
    <i class="bi bi-info-circle" 
       data-bs-toggle="tooltip" 
       title="Максимум 30 символов"></i>
</label>
```

```javascript
// Инициализация tooltips
var tooltipTriggerList = [].slice.call(
    document.querySelectorAll('[data-bs-toggle="tooltip"]')
);
tooltipTriggerList.map(function (tooltipTriggerEl) {
    return new bootstrap.Tooltip(tooltipTriggerEl);
});
```

### Динамический расчет стоимости

```javascript
quantityInput.addEventListener('input', function() {
    const quantity = parseInt(this.value) || 0;
    const basePrice = parseFloat(basePriceInput.value) || 0;
    const totalPrice = basePrice * quantity;
    
    const formattedPrice = totalPrice.toLocaleString('ru-RU', {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2
    });
    
    totalHint.innerHTML = 
        '<strong>Итоговая сумма:</strong> ' + 
        formattedPrice + ' руб. ' +
        '<small>(до применения бонусов)</small>';
});
```

### Визуальная обратная связь

```html
<small class="form-text text-muted">
    Пример: 79123456789
</small>
```

---

## 4. Безопасность

### XSS защита
```php
<?= htmlspecialchars($_POST['login'] ?? '') ?>
```

### SQL-инъекции
```php
$stmt = $pdo->prepare("SELECT * FROM user_ WHERE login = ?");
$stmt->execute([$login]);
```

### CSRF защита
- Используется `session_start()` для проверки сессий
- POST-запросы обрабатываются только для авторизованных пользователей

---

## 5. Тестовые сценарии

### Тест 1: Регистрация с валидными данными
```
Логин: john_doe
Пароль: Pass123!
Телефон: 79123456789
→ ✅ Регистрация успешна
```

### Тест 2: Регистрация с невалидным телефоном
```
Телефон: 89123456789
→ ❌ "Номер телефона должен быть в формате 79xxxxxxxxx"
```

### Тест 3: Регистрация с кириллицей в логине
```
Логин: пользователь
→ ❌ "Логин должен содержать только английские буквы"
```

### Тест 4: Заказ с большим количеством
```
Количество: 2000000
→ ❌ "Количество не может превышать 1 000 000 единиц"
```

### Тест 5: Заказ с переполнением стоимости
```
Цена: 10000
Количество: 1000000
→ ❌ "Стоимость заказа слишком велика"
```

### Тест 6: Заказ без адреса
```
Адрес: не выбран
→ ❌ "Необходимо выбрать адрес организации"
```

---

## 6. Примеры сообщений об ошибках

### Регистрация
- ✅ Понятные: "Номер телефона должен быть в формате 79xxxxxxxxx"
- ✅ Конкретные: "Логин не должен превышать 30 символов"
- ✅ С примерами: "Пример: 79123456789"

### Оформление заказа
- ✅ С лимитами: "Количество не может превышать 1 000 000 единиц"
- ✅ С решениями: "Для больших заказов свяжитесь с администратором"
- ✅ С форматированием: "Максимальная сумма: 2 147 483 647 руб."

---

## 7. Константы и лимиты

```php
// Регистрация
const MAX_LOGIN_LENGTH = 30;
const MIN_PASSWORD_LENGTH = 6;
const PHONE_FORMAT = '/^79\d{9}$/';

// Заказы
const MAX_QUANTITY = 1000000;          // 1 миллион единиц
const MAX_INT = 2147483647;            // MySQL INT max
const MAX_PRICE = 2147483647;          // Максимальная стоимость (руб.)
```

---

## 8. Файлы изменений

### Обновлены:
- ✅ `register/index.php` - валидация регистрации
- ✅ `orders/create.php` - валидация заказов

### Создано:
- ✅ `database/VALIDATION.md` - этот документ

---

## 9. Checklist внедрения

- [x] Серверная валидация телефона (79xxxxxxxxx)
- [x] Серверная валидация логина (только английские)
- [x] Серверная валидация пароля (только английские + спецсимволы)
- [x] Клиентская валидация с HTML5 patterns
- [x] JavaScript валидация в реальном времени
- [x] Автоформатирование телефона
- [x] Проверка уникальности логина/телефона
- [x] Валидация количества услуг (лимит 1,000,000)
- [x] Проверка переполнения стоимости
- [x] Обязательный выбор адреса
- [x] Tooltips с подсказками
- [x] Динамический расчет стоимости
- [x] Понятные сообщения об ошибках
- [x] XSS/SQL защита

---

**Дата:** 2026-02-10  
**Версия:** 1.0  
**Статус:** ✅ Внедрено
