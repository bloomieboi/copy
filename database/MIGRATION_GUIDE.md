# Миграция на безопасное хранение паролей

## ⚠️ ВАЖНО: Обязательные шаги

### Шаг 1: Обновите тип поля password в БД

Выполните SQL-скрипт через phpMyAdmin:

```sql
USE copypasteDB;

ALTER TABLE `user_` 
MODIFY COLUMN `password` VARCHAR(255) NOT NULL;
```

**Или используйте готовый файл:**
1. Откройте phpMyAdmin
2. Выберите базу `copypasteDB`
3. Вкладка "SQL"
4. Скопируйте содержимое `database/migrate_password_hash.sql`
5. Вставьте и выполните

### Шаг 2: Проверьте работу системы

1. **Вход существующих пользователей:**
   - Старые пароли (SHA1) продолжат работать
   - При входе они автоматически обновятся на bcrypt
   - Никаких действий от пользователей не требуется

2. **Регистрация новых пользователей:**
   - Сразу получат bcrypt хеш
   - Максимальная безопасность с первого дня

3. **Смена пароля администратором:**
   - Поле пароля всегда пустое
   - Требуется подтверждение
   - Администратор НЕ МОЖЕТ узнать текущий пароль

## ✅ Что было исправлено

### Проблемы безопасности (ИСПРАВЛЕНО):
- ❌ ~~Использовался устаревший SHA1~~
- ❌ ~~Отсутствовала соль (salt)~~
- ❌ ~~Администратор мог просматривать пароли~~

### Реализованные решения:
- ✅ Современный bcrypt с автоматической солью
- ✅ Защита от timing attacks
- ✅ Автоматическое перехеширование
- ✅ Обратная совместимость
- ✅ Валидация и подтверждение паролей
- ✅ Информирование администратора

## 🔍 Проверка миграции

### Тест 1: Проверьте тип поля
```sql
DESCRIBE user_;
```
**Ожидаемо:** `password` VARCHAR(255)

### Тест 2: Войдите существующим пользователем
- Вход должен работать со старым паролем
- После входа проверьте БД - хеш должен обновиться

### Тест 3: Зарегистрируйте нового пользователя
- Проверьте в БД - хеш должен быть длиннее 40 символов

### Тест 4: Попробуйте изменить пароль как администратор
- Поле должно быть пустым
- Должно требоваться подтверждение
- После сохранения - новый хеш в БД

## 📊 Статистика хешей

Проверьте распределение типов хешей в вашей БД:

```sql
SELECT 
    CASE 
        WHEN LENGTH(password) = 40 THEN 'SHA1 (старый)'
        WHEN LENGTH(password) > 40 THEN 'Bcrypt (новый)'
        ELSE 'Неизвестный'
    END AS hash_type,
    COUNT(*) AS count
FROM user_
GROUP BY hash_type;
```

## 🚨 Возможные проблемы

### Проблема: "Data too long for column 'password'"
**Решение:** Вы не выполнили миграцию БД (Шаг 1)

### Проблема: Пользователи не могут войти
**Решение:** Проверьте, что код обратной совместимости на месте в `login/index.php`

### Проблема: В форме редактирования виден хеш пароля
**Решение:** Обновите `admin/user_edit.php` - поле должно быть БЕЗ value

## 📝 Дополнительная информация

- **Документация:** `database/PASSWORD_SECURITY.md`
- **README:** Обновлен раздел "Безопасность"
- **Поддержка:** Проверьте логи ошибок PHP при проблемах

---

**Дата миграции:** 2026-02-09  
**Версия системы:** 1.1  
**Критичность:** ВЫСОКАЯ (безопасность)
