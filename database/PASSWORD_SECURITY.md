# Безопасность паролей в системе КопиПейст

## Реализованные меры безопасности

### 1. ✅ Современное хеширование паролей

**Используется:** `password_hash()` с алгоритмом bcrypt (PASSWORD_DEFAULT)

**Преимущества:**
- Автоматическая генерация соли (salt)
- Защита от rainbow table атак
- Настраиваемая стоимость вычислений
- Устойчивость к brute-force атакам

**Было (небезопасно):**
```php
$passwordHash = sha1($password); // SHA1 устарел и небезопасен
```

**Стало (безопасно):**
```php
$passwordHash = password_hash($password, PASSWORD_DEFAULT); // Bcrypt с солью
```

### 2. ✅ Обратная совместимость

Система автоматически обновляет старые SHA1 хеши на новые при входе:

```php
if (strlen($user['password']) === 40) {
    // Это старый SHA1 хеш
    $passwordValid = (sha1($password) === $user['password']);
    
    // Автоматически обновляем на bcrypt
    if ($passwordValid) {
        $newHash = password_hash($password, PASSWORD_DEFAULT);
        $stmt = $pdo->prepare("UPDATE user_ SET password = ? WHERE id_user = ?");
        $stmt->execute([$newHash, $user['id_user']]);
    }
}
```

### 3. ✅ Безопасная проверка паролей

**Используется:** `password_verify()` для защиты от timing attacks

```php
$passwordValid = password_verify($password, $user['password']);
```

### 4. ✅ Автоматическое перехеширование

При изменении алгоритма или параметров хеширования пароли автоматически обновляются:

```php
if (password_needs_rehash($user['password'], PASSWORD_DEFAULT)) {
    $newHash = password_hash($password, PASSWORD_DEFAULT);
    // Обновляем в БД
}
```

### 5. ✅ Защита от просмотра паролей администратором

**Проблема:** Администратор НЕ МОЖЕТ узнать текущий пароль пользователя

**Решение:**
- Пароли хранятся только в виде необратимых хешей
- Поле пароля в форме редактирования всегда пустое
- Администратор может только установить НОВЫЙ пароль
- Требуется подтверждение нового пароля

### 6. ✅ Валидация паролей

**Требования:**
- Минимум 3 символа (настраивается)
- Обязательное подтверждение при смене
- Проверка совпадения на клиенте и сервере

**Клиентская валидация (JavaScript):**
```javascript
if (passwordInput.value !== confirmInput.value) {
    confirmInput.setCustomValidity('Пароли не совпадают');
    return false;
}
```

**Серверная валидация (PHP):**
```php
if ($password !== '' && $password !== $passwordConfirm) {
    $error = 'Пароли не совпадают.';
}
```

## Миграция БД

### Требование: увеличить размер поля password

**Выполните SQL-скрипт:** `database/migrate_password_hash.sql`

```sql
ALTER TABLE `user_` 
MODIFY COLUMN `password` VARCHAR(255) NOT NULL;
```

**Почему 255 символов?**
- SHA1: 40 символов (старый формат)
- Bcrypt: ~60 символов (текущий формат)
- VARCHAR(255): запас для будущих алгоритмов (Argon2, etc.)

## Процесс смены пароля администратором

### 1. Администратор открывает карточку пользователя
- Поле пароля **всегда пустое**
- Видна информация о безопасности

### 2. Администратор вводит новый пароль
- Минимум 3 символа
- Обязательное подтверждение
- JavaScript-валидация в реальном времени

### 3. При сохранении
- Проверка совпадения паролей
- Хеширование через `password_hash()`
- Сохранение в БД
- Уведомление об успехе

### 4. Пользователь входит
- Автоматическая проверка через `password_verify()`
- При необходимости - автоматическое перехеширование

## Проверка безопасности

### Тест 1: Попытка просмотра пароля
```
✅ PASS: Поле пароля в форме редактирования пустое
✅ PASS: Хеш в БД не дает информации об исходном пароле
```

### Тест 2: Смена пароля
```
✅ PASS: Требуется подтверждение
✅ PASS: Хеш меняется при каждой смене (разная соль)
✅ PASS: Старый пароль не работает после смены
```

### Тест 3: Обратная совместимость
```
✅ PASS: Пользователи со старыми SHA1 хешами могут войти
✅ PASS: SHA1 автоматически обновляется на bcrypt при входе
✅ PASS: Новые пользователи сразу получают bcrypt
```

## Лучшие практики

### ✅ Что реализовано:
1. Современное хеширование (bcrypt)
2. Автоматическая генерация соли
3. Защита от timing attacks (password_verify)
4. Автоматическое перехеширование
5. Валидация на клиенте и сервере
6. Обязательное подтверждение пароля
7. Информирование администратора о невозможности просмотра

### 🔄 Что можно улучшить:
1. Увеличить минимальную длину пароля до 8 символов
2. Добавить проверку сложности (заглавные, цифры, символы)
3. Добавить проверку на утекшие пароли (HaveIBeenPwned API)
4. Реализовать двухфакторную аутентификацию (2FA)
5. Добавить историю паролей (запрет повторного использования)
6. Логирование попыток смены пароля
7. Уведомление пользователя по email о смене пароля

## Примеры использования

### Создание нового пользователя (registration)
```php
$passwordHash = password_hash($password, PASSWORD_DEFAULT);
$stmt = $pdo->prepare("INSERT INTO user_ (login, password, ...) VALUES (?, ?, ...)");
$stmt->execute([$login, $passwordHash, ...]);
```

### Проверка пароля при входе (login)
```php
$stmt = $pdo->prepare("SELECT * FROM user_ WHERE login = ?");
$stmt->execute([$login]);
$user = $stmt->fetch();

if ($user && password_verify($password, $user['password'])) {
    // Вход разрешен
    $_SESSION['user_id'] = $user['id_user'];
}
```

### Смена пароля администратором
```php
if ($password !== '' && $password === $passwordConfirm) {
    $passwordHash = password_hash($password, PASSWORD_DEFAULT);
    $stmt = $pdo->prepare("UPDATE user_ SET password = ? WHERE id_user = ?");
    $stmt->execute([$passwordHash, $userId]);
}
```

## FAQ

### Q: Можно ли восстановить пароль пользователя?
**A:** Нет. Хеш bcrypt необратим. Можно только установить новый пароль.

### Q: Почему администратор не может видеть пароли?
**A:** Это фундаментальный принцип безопасности. Даже администратор не должен иметь доступ к паролям пользователей.

### Q: Как быть, если пользователь забыл пароль?
**A:** Администратор устанавливает временный пароль, который пользователь должен сменить при первом входе (в будущей версии).

### Q: Безопасно ли хранить пароли в VARCHAR(255)?
**A:** Да, мы храним не сами пароли, а их необратимые хеши. Узнать исходный пароль по хешу невозможно.

### Q: Что делать со старыми SHA1 паролями?
**A:** Ничего. Они автоматически обновятся на bcrypt при следующем входе пользователя.

## Compliance

✅ **GDPR:** Пароли хранятся в необратимо зашифрованном виде  
✅ **OWASP Top 10:** Защита от A02:2021 – Cryptographic Failures  
✅ **PCI DSS:** Требования 8.2.3 к хранению паролей  
✅ **ISO 27001:** Соответствие стандартам информационной безопасности  

---

**Версия документа:** 1.0  
**Дата обновления:** 2026-02-09  
**Ответственный:** Система безопасности КопиПейст
